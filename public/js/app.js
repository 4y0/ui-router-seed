'use strict';

// Declare app level module which depends on filters, and services

/**
 * Modified Angular SEED APP 
 *
 */
(function () {
    angular.module('myApp', [
        'ui.router', 
    ])
})();
